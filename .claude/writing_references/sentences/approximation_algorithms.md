# Approximation Algorithms Sentence Templates

Sentence patterns for approximation algorithms and computational complexity papers.

---

## [Motivation from Hardness] Approximation algorithms naturally arise as a consequence of the widely believed P ≠ NP conjecture. Under this conjecture, a wide class of [problems] cannot be solved exactly in polynomial time.

**Source**: Approximation algorithms literature
**Context**: Motivating the need for approximation

> Approximation algorithms naturally arise in the field of theoretical computer science as a consequence of the widely believed P ≠ NP conjecture. Under this conjecture, a wide class of optimization problems cannot be solved exactly in polynomial time.

**Tags**: #motivation #complexity #hardness #approximation

---

## [Field Goal] The field of [approximation algorithms] therefore tries to understand how closely it is possible to approximate optimal solutions to such problems in polynomial time.

**Source**: Approximation algorithms literature
**Context**: Stating research goal

> The field of approximation algorithms therefore tries to understand how closely it is possible to approximate optimal solutions to such problems in polynomial time.

**Tags**: #goal #field #approximation #complexity

---

## [Definition of Approximation] Approximation algorithms are efficient algorithms that find approximate solutions to [problems] with provable guarantees on the distance of the returned solution to the optimal one.

**Source**: Approximation algorithms textbooks
**Context**: Defining approximation algorithms

> Approximation algorithms are efficient algorithms that find approximate solutions to optimization problems (in particular NP-hard problems) with provable guarantees on the distance of the returned solution to the optimal one.

**Tags**: #definition #guarantee #approximation #algorithms

---

## [PTAS Definition] A polynomial-time approximation scheme (PTAS) is an algorithm which takes an instance and a parameter ε > 0 and produces a solution within a factor 1 + ε of optimal.

**Source**: PTAS literature
**Context**: Defining PTAS

> A polynomial-time approximation scheme (PTAS) is a type of approximation algorithm for optimization problems. A PTAS is an algorithm which takes an instance and a parameter ε > 0 and produces a solution that is within a factor 1 + ε of being optimal.

**Tags**: #definition #ptas #scheme #approximation

---

## [FPTAS as Holy Grail] Fully polynomial-time approximation schemes are the holy grail of approximation algorithms; they do not appear to exist for many problems, but when available, they are almost as useful as an optimizing algorithm.

**Source**: FPTAS literature
**Context**: Positioning FPTAS importance

> Fully polynomial-time approximation schemes are the holy grail of approximation algorithms; they do not appear to exist for many problems, but when they are available, they are often almost as useful as an optimizing algorithm would be.

**Tags**: #fptas #holy_grail #importance #approximation

---

## [Strongest Result] With respect to worst-case approximation, an FPTAS is the strongest possible result that we can derive for an NP-hard problem.

**Source**: FPTAS literature
**Context**: Stating optimality of FPTAS

> With respect to worst-case approximation, an FPTAS is the strongest possible result that we can derive for an NP-hard problem, as it provides arbitrarily good approximation in polynomial time.

**Tags**: #fptas #strongest #result #approximation

---

## [Hardness of Approximation] NP-hard problems vary greatly in their approximability; some can be approximated within any ε > 0, while others are impossible to approximate within any constant factor unless P = NP.

**Source**: Approximation hardness literature
**Context**: Stating complexity landscape

> NP-hard problems vary greatly in their approximability; some, such as the knapsack problem, can be approximated within any ε > 0, while others are impossible to approximate within any constant factor unless P = NP, as in the case of the maximum clique problem.

**Tags**: #hardness #landscape #inapproximability #approximation

---

## [Complexity Hierarchy] It is easy to see that [class A] ⊆ [class B] ⊆ [class C], and unless P = NP, these inclusions are strict.

**Source**: Complexity theory
**Context**: Stating class relationships

> It is easy to see that FPTAS ⊆ PTAS ⊆ APX ⊆ NPO, and unless P = NP, these inclusions are strict.

**Tags**: #hierarchy #complexity #classes #approximation

---

## [Inapproximability Result] Any [strongly NP-hard] optimization problem with [condition] cannot have an [approximation scheme] unless P = NP.

**Source**: Inapproximability literature
**Context**: Stating impossibility result

> Any strongly NP-hard optimization problem with a polynomially bounded objective function cannot have an FPTAS unless P = NP.

**Tags**: #inapproximability #impossibility #hardness #approximation

---

## [Approximation Ratio] We design an algorithm that achieves an approximation ratio of [α], meaning the solution value is within a factor [α] of optimal.

**Source**: Approximation algorithms papers
**Context**: Stating approximation guarantee

> We design an algorithm that achieves an approximation ratio of α, meaning the solution value is within a factor α of optimal for all instances.

**Tags**: #ratio #guarantee #result #approximation

---
